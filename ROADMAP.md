# Roadmap

- **Publish to PyPI** — make the connector pip-installable through `requirements.txt`,
  so it can be deployed like any other Python dependency.
- **Add a CLI layer** — allow administrators to pair an instance and run a sync from the
  command line, without installing the Odoo module in the database.
